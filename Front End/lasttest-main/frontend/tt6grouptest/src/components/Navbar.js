import * as React from "react";
import { useNavigate } from "react-router-dom";

const pages = ["Dashboard", "Wallet", "Exchange Rate", "Account"];


const Navbar = () => {

    const navigate = useNavigate();

    return (

    );
}

export default Navbar;